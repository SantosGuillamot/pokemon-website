# Spec Review

## Verdict: approved

## Reviewed revision

`9395916` (`Adjudicate spec review 1: fix insufficient-data edge case to mirror model`), branch `feature/special-attack`.

## Verification log

Independent full re-review (prior checks not assumed). All checks are read-only re-executions against the worktree code; the worktree was clean before and after (no writes until this review file).

1. **Routing / server-side data (Req 1).** Read `src/routes/index.ts`. `/speeds` is registered in the `pages` array (lines 33-38) with `scripts: ["/js/stores/pages/speeds.js"]`; the loop (54-61) runs `resetServerState()`, `await loadTypes()`, `await loadPokemons()`, then renders `Layout`. `/team-building` (63-80) is the sole handler loading extra reference data. Confirms a mirrored page reaches its own URL with Pokémon data loaded server-side. Matches R1 routing.
2. **Global nav (Req 2, Req 13).** Read `src/components/Nav.ts`. `GAME_LINKS` (6-11) holds `{ label: "SPEEDS", href: "/speeds" }`, rendered in both the desktop GAMES dropdown (102-116) and the mobile Games group (176-190). Shared array → adding one entry updates nav site-wide (intended per Req 2/13). Matches R1 navigation.
3. **Homepage card (Req 2).** Read `src/pages/home.ts`. `GAMES` array (14-43) contains the "Who's Faster" entry (22-28) with `href: "/speeds"`, `icon: Timer`, rendered as a `Card` in the "Games" `Section`. Matches R1.
4. **Page shell + section selector (Req 3, Req 4).** Read `src/pages/speeds.ts`. Hero (84-90); wrapper `data-wp-context='{"currentSection": null}'` (92-94); "Learn Speeds" selector with three `SectionCard`s (96-117); sections bind `data-wp-bind--hidden="!state.isCurrentSection"`. Read `src/client/stores/section-utils.ts`: `isCurrentSection` = `currentSection === sectionId`; `selectSection` sets `currentSection`. On load `currentSection` is null → only hero + selector visible; a card reveals exactly one section. Matches R1 shell.
5. **Head-to-head quiz, reveal, reduced motion, streak, ties, restart (Req 5-9).** Read `src/client/stores/pages/speeds.ts`. `storeAnswer` (226-243) sets `correctAnswer = "tie"` when speeds equal else `0|1`; `guessSpeed` (170-183) treats tie OR matching index as correct (confirms Req 8). `revealAnswer` (66-123): count-up 0→actual over 1000 ms with cubic ease-out `1 - (1 - progress) ** 3`; `prefers-reduced-motion` sets `animationProgress` to 1 immediately (confirms Req 6). Correct → `streak += 1`, auto-advance after 1000 ms via `randomize`; incorrect → `finalStreak = streak`, `streak = 0`, `quizState = "incorrect"` (confirms Req 7). Read `src/components/QuizSection.ts` + `src/components/QuizStatus.ts`: "Streak: N" shown while not incorrect (confirms Req 9); "GAME OVER / Final streak: N / Try again" (restart control) on incorrect (confirms Req 7 restart). Server seeds two distinct random Pokémon side by side (`pickTwo`, speeds.ts:65, 133-135) (confirms Req 5). Matches R1 3c.
6. **Data table (Req 10).** Read `src/pages/speeds.ts` (191-212): columns sprite / Name (sortable+searchable) / Speed (sortable); `searchMode: "scroll"`, `sortColumn: "speed"`, `sortDirection: "desc"`. Read `src/client/stores/data-table.ts`: filter is skipped when `searchMode === "scroll"` (line 40) — all rows kept; numeric columns sorted numerically (63-65); `toggleSort` (118-128) toggles asc/desc; `isRowHighlighted` (102-114) marks matches; `scrollToSearchMatch` (152-183) scrolls the first match into view. Confirms default stat-desc sort, sort-by-column toggle, and search = scroll+highlight (not filter). Matches R1 3e.
7. **Data provenance + client feasibility (Req 11).** Read `src/db/schema/pokemons.ts`: `spAttack: smallint("sp_attack").notNull()` (line 24), a direct peer of `speed` (26). Read `src/types/pokemons.ts`: `Pokemon = InferSelectModel<typeof pokemons>` (line 4) → `spAttack` on the type. Read `src/api/pokemons.ts`: `db.select().from(pokemons)` with no column projection (49-57) → full rows. Read `src/client/stores/pokemons.ts`: `pokemons: Record<string, Pokemon>`; `getPokemon`/`getRandomPokemons` return full `Pokemon` objects — so `.spAttack` is present client-side just as `.speed` is. `grep -rn spAttack src` confirms the field is real and already used (`MetaPokemonsSection.ts:48`, `design-system.ts:28,543`, team-builder types). Confirms the mirror needs no new data source/query/schema and is feasible (client store already carries the value the quiz would read). Matches R1 data.
8. **Naming label (copy default, not load-bearing).** `grep` confirms `src/pages/design-system.ts:543` and `src/components/MetaPokemonsSection.ts:48` both use `{ key: "spAttack", label: "SpA", sortable: true }` — the "SpA" abbreviation precedent from R2.
9. **Out-of-scope exclusions.** `moves-priority` placeholder present and unreachable (`speeds.ts:214-222`; no control sets `currentSection` to it) → OOS 2. Numeric "Guess Speed" is a full sibling game (`speeds.ts:140-189`, store `submitGuessSpeed` 195-207) that the intent's enumerated directions omit → OOS 1 is a defensible dispositioning of an open direction the intent delegated to the agents. Shipped table has no dex column → OOS 3. Matches R1/R2.
10. **Insufficient-data edge case (Req 12) — prior finding, now RESOLVED.** Read `src/pages/speeds.ts:62-64`: `if (pokemons.length < 2) { return html\`<main><p>Not enough Pokemon loaded.</p></main>\`; }` — a whole-page early return before the Hero, selector, quiz sections, and table; no error thrown. spec.md Req 12 (line 22) and its AC (line 46) now read "renders only a graceful 'not enough Pokémon' message — no hero, section selector, or table — and does not error, mirroring the model page's whole-page early return," which matches the model exactly. Grounding chain closed: `spec-research.md` now carries an R1 "Insufficient-data guard" finding (line 39) plus an evidence bullet (line 60) citing `src/pages/speeds.ts:61-64`, and consolidated Req 12 (line 102) cites `(R1: insufficient-data guard)`. No divergence remains.
11. **Adjudication scope check.** `git diff 9220f36..9395916` touches only the three spec-phase artifacts (spec.md, spec-research.md, the added review-1 file); no source code changed. The edits are confined to Req 12, its AC, and the R1 grounding — nothing else regressed.

## Summary

On an independent full re-review, all 13 requirements faithfully and verifiably mirror the shipped "Who's Faster?"/Speeds model page: routing and server-side data, discoverability (shared nav array + homepage card), the section-selector shell, the head-to-head quiz (reveal animation, reduced-motion fallback, streak/tie/game-over/restart), the sortable-searchable scroll-mode table, and the "existing `spAttack` data only" boundary all re-executed cleanly against the codebase, and the client pokemon store already carries `spAttack`, so the mirror is feasible with no new data work. The open reuse-vs-generalize direction and the drop of the numeric-guess sub-game are correctly deferred/dispositioned (OOS 5, OOS 1). The sole prior finding — Requirement 12's insufficient-data behavior contradicting the model and lacking recorded grounding — is fully resolved: the requirement and its acceptance criterion now describe the model's actual whole-page early return (verified at `src/pages/speeds.ts:62-64`), and the research now records that behavior with evidence and grounds Req 12 in it. Coverage, altitude, scope, acceptance criteria, and spec-to-research fidelity all hold, and negative-space checks surface no feasibility contradiction or unnamed must-preserve behavior beyond Req 13. Approved.

## Issues

None.
